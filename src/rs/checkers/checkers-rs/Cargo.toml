[package]
name = "checkers-rs"
version = "0.1.0"
authors = ["Yaroslav <q.link0.p@gmail.com>"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0.125", features = ["derive"] }
num_enum = "0.5.1"
